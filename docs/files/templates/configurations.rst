.. _templates.configurations:

################
Configurations
################

The code below shows an example of a full `config.yaml` file. This file is used to 
configure the ZEN-creator and is required for it to run. 

.. literalinclude:: ../generated/default_config_example.yaml
   :language: yaml
